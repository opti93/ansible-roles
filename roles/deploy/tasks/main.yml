---
- name: Создание Сети
  community.docker.docker_network:
    name: app_network
    driver: overlay

- name: Выкладка сервиса
  import_tasks: "../services/{{ item }}/service.yml"
  Vars:
    - name: "{{ item }}"
  loop: "{{ services }}"