---
- name: Создание Сети
  community.docker.docker_network:
    name: app_network
    driver: overlay

- name: Выкладка сервиса
  include_tasks: "../services/{{ item }}/service.yml"
  vars:
    name: "{{ item }}"
  loop: "{{ services }}"